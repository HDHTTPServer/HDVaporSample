#extend("base")

#export("title") { HDVaporSample }

#export("content") {
    <div class="container" id="app">
        <div id="main" style="max-height:800px; overflow-y:auto;" v-cloak>
            <span v-for="result in results">{{ result.data }}, </span>
        </div>
    </div>
    <script src="app.js"></script>
}
