#extend("base")

#export("title") { HDVaporSample }

#export("content") {
    <div class="container" id="app">
        <span v-for="result in results">
            <span v-cloak>{{ result.data }}, </span>
        </span>
    </div>
    <script src="app.js"></script>
}
